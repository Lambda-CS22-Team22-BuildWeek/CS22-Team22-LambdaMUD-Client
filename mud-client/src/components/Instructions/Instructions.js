import React from "react";

const Instructions = () => {
    return (
        <div>
            <p>
                Use the arrows to navigate your way through the dungeon. 
                We can add some other instructions here should we so choose like look for items etc.
            </p>
        </div>
    );
};

export default Instructions;